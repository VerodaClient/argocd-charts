apiVersion: projectcalico.org/v3
kind: GlobalNetworkPolicy
metadata:
  labels:
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: calico-ee-global-netpol-headroom-ext-prod-canary-15
    app.upwork.com/runtime: eks
  name: allow-from-fluentd-15
spec:
  egress:
    - action: Allow
      destination:
        selector: all()
      source:
        selector: app.kubernetes.io/name == 'fluentd'
  order: 5
  selector: app.kubernetes.io/name == 'fluentd'
  types:
    - Egress
