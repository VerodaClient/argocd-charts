apiVersion: projectcalico.org/v3
kind: GlobalNetworkPolicy
metadata:
  labels:
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: calico-ee-global-netpol-headroom-ext-prod-canary-82
    app.upwork.com/runtime: eks
  name: allow-from-prometheus-82
spec:
  egress:
    - action: Allow
      destination:
        selector: all()
      source:
        selector: app.kubernetes.io/name == 'prometheus'
  order: 5
  selector: app.kubernetes.io/name == 'prometheus'
  types:
    - Egress
