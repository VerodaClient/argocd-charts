apiVersion: projectcalico.org/v3
kind: GlobalNetworkPolicy
metadata:
  labels:
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: calico-ee-global-netpol-headroom-ext-prod-canary-96
    app.upwork.com/runtime: eks
  name: default.allow-from-etcd-96
spec:
  egress:
    - action: Allow
      destination:
        selector: all()
      source:
        selector: app.kubernetes.io/name == 'etcd'
  order: 5
  selector: app.kubernetes.io/name == 'etcd'
  types:
    - Egress
